{% extends 'base.html.twig' %}

{% block body %}
    <form action="{{ path('getProject') }}">
        <div class="row">
            <div class="form-group col-lg-8">
                <label for="affaire">Numéro affaire</label>
                <input id="affaire" name="affaire" type="text">
            </div>
            <div class="form-group col-lg-4">
                <input type="submit">
            </div>
        </div>
    </form>
{% endblock %}